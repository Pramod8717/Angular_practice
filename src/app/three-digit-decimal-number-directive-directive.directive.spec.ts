import { ThreeDigitDecimalNumberDirectiveDirectiveDirective } from './three-digit-decimal-number-directive-directive.directive';

describe('ThreeDigitDecimalNumberDirectiveDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeDigitDecimalNumberDirectiveDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
