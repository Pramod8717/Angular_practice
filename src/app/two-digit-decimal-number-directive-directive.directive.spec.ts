import { TwoDigitDecimalNumberDirectiveDirectiveDirective } from './two-digit-decimal-number-directive-directive.directive';

describe('TwoDigitDecimalNumberDirectiveDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoDigitDecimalNumberDirectiveDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
